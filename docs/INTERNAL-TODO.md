# Internal TODO - AMD Agentic AI Profiling Workshop

Internal working backlog for the workshop maintainers (Shailen + Hermes).
Not participant-facing. Check items off as they land; keep the newest context
with each item so anyone can pick it up cold.

## Open

### 1. Notebook: add an Edge TTS vs Kokoro comparison table
- **What:** In the workshop notebook (`tts.ipynb`), add a markdown comparison
  table that contrasts **Edge TTS** (the cloud baseline) with **Kokoro** (the
  local model we switch to).
- **Why:** Right now the notebook swaps Edge TTS for Kokoro without spelling out
  the concrete reasons. The table should make the motivation explicit for
  participants.
- **Table should cover:**
  - **Limitations of Edge TTS** that push us toward a local model, e.g.:
    - length / duration caps on a single TTS request (long inputs get cut or
      must be chunked)
    - dependence on a cloud service (network round-trip, rate limits, no offline
      use, availability/ToS constraints)
    - no GPU utilization / nothing to profile or optimize locally (defeats the
      workshop's point)
    - limited control over batching and throughput
  - **Reasons for trying Kokoro** (the counterpoints):
    - runs locally on the MI300X -> real GPU telemetry to profile and optimize
    - no per-request length ceiling the way the cloud API imposes
    - enables the batched-inference optimization that is the core lesson
    - reproducible, offline-capable, no external dependency
- **Placement:** near the point in the notebook where Edge TTS is introduced /
  right before Kokoro is brought in, so the table motivates the switch.
- **Verify the specifics before writing them as fact:** confirm the actual Edge
  TTS length/duration limit (measure it or cite the source) rather than stating
  a number from memory; the workshop's standard is measured evidence, not
  plausible-sounding figures.
- **Regenerate, don't hand-edit:** `tts.ipynb` is generated by
  `scripts/build_tts_notebook.py`. Add the table there so the notebook stays
  reproducible from its source; do not hand-edit the committed notebook only.
- **Requested by:** Shailen, 2026-09-16.
- **Status:** not started.

## Done

(none yet)
